You are tasked with creating a function that takes a simple representation of an instruction format and returns a tuple containing a mnemonic and a list of parameters. The function should accept two inputs: an integer `imm16` (representing an immediate value for the instruction) and two integers `reg1` and `reg2` (representing register identifiers). The function should return a tuple where the first element is the string 'ORI', and the second element is a list containing the immediate value as a non-negative integer, and the two register identifiers. The immediate value should be represented as a dictionary with keys 'value' and 'width', where 'value' is the integer value and 'width' is always 16. The registers should be represented as dictionaries as well, with a key 'id' for their identifiers. The function signature should be: `def instruction_format(imm16: int, reg1: int, reg2: int) -> Tuple[str, List[Dict[str, Union[int, str]]]]:`

---
Write your Python solution to the file `/app/solution.py`. It must define the required function(s) at module top level so they can be imported and called directly.
